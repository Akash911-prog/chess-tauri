const Menu = () => {
    return (
        <div>
            <h1 className="text-5xl">Chess On Cracks</h1>
            // TODO: start here. Sort the heading. add motion. and prob make the
            heading a svg or img rather than text
        </div>
    );
};

export default Menu;
